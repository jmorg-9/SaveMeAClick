{
    "name": "clickbait-crusader",
    "private": true,
    "workspaces": ["api", "ui"],
    "scripts": {
      "dev": "concurrently \"npm run dev --workspace api\" \"npm run dev --workspace ui\"",
      "build": "npm run build --workspace api && npm run build --workspace ui",
      "lint": "eslint . --ext .ts,tsx",
      "test": "jest"
    },
    "devDependencies": {
      "concurrently": "^7.0.0",
      "eslint": "^8.0.0",
      "typescript": "^5.0.0"
    }
  }
  